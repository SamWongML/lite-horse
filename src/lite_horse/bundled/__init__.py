"""Image-shipped layer-0 entities (skills, instructions, commands).

These are read-only at runtime; the resolver assembles them with the
official (DB) and user (DB) layers per the v0.4 layered-config rules.
"""
