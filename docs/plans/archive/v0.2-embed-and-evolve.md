# lite-horse v0.2 — embed in webapp + strengthen self-evolution

**Status:** SHIPPED
**Started:** 2026-04-20
**Shipped:** 2026-04-23
**Predecessor:** [v0.1-hermes-port.md](v0.1-hermes-port.md)
**Status ledger:** [../PROGRESS.md](../PROGRESS.md)

Each phase is sized for one engineer, one PR. Check boxes as acceptance
criteria land. Do not reorder phases without coordinating — several have hard
dependencies noted under **Depends on:**.

---

## Objective

Ship lite-horse as an **embeddable Python package** consumed by a single
project-management web backend, with a stronger self-evolution loop than v0.1
(compression-as-consolidation, periodic nudges, in-use skill refinement,
offline GEPA-style evolve). Target: **≤ 2,000 runtime LOC** after drops.

## Non-goals (explicit cuts)

- Telegram / Discord / Slack / any chat platform adapter
- CLI REPL as a product surface (keep a tiny debug entry only)
- systemd unit files shipped in the repo
- Honcho dialectic user modeling (heavy; `USER.md` covers 80 %)
- Subagent spawning as a tool
- Additional model providers (OpenAI only — matches v0.1 rule)
- 40-tool zoo; webapp-specific tools come in via MCP, not builtins

## Invariants — do not regress

1. `openai-agents >= 0.14.1, < 0.15`; no Anthropic SDK anywhere
2. Python 3.11+; `uv` for env; ruff + mypy strict + pytest must stay green
3. No orphan `asyncio.create_task` — every long-lived task has a supervisor
4. Instructions are a **frozen snapshot** at session start
5. **Prompt-cache-stable block order** (see Phase 13)
6. Any string that ends up in the system prompt must pass
   `_INJECTION_PATTERNS` + `_INVISIBLE_RE` validation
7. Memory char limits (2200 / 1375) and skill size cap (15 KB) enforced
8. No MCP URL accepted from user input — config/code only
9. Evolution never breaks the user's run — all distiller failures swallowed
10. Webapp is the only consumer; no public Python API surface outside
    `lite_horse.api`

## Phase ordering and dependency graph

```
13 (fixes) ─┬─► 14 (skill_view) ─┐
            ├─► 15 (drops)       ├─► 16 (api) ─► 17 (cron webhook) ─► 22 (errors)
            ▼                    ▼                                     │
18 (consolidation) ─► 19 (nudges) ─► 20 (stats) ─► 21 (conditional)   │
                                           │                           │
                                           ▼                           ▼
                                     24 (evolve) ◄──── 23 (mcp/cleanup)
                                                                       │
                                                                       ▼
                                                                25 (hardening)
```

Ship **13 → 14 → 15 → 16 → 17** as one sprint (foundation). Everything else
can parallelize after that.

---

## Phase 13 — prompt-cache fix + skill-write injection defense

**Objective:** remove the two cheap-but-high-impact cliffs from v0.1.
**Why:** `Current time: {now}` at prompt block #2 invalidates the OpenAI
prompt cache every turn (every second, actually). `skill_manage.create/edit`
accepts unvalidated content that is later concatenated into the system prompt
via `_skills_index()` — classic prompt-injection sink.
**Depends on:** none. Do this first.

### Deliverables
- [x] `src/lite_horse/agent/instructions.py` — move `f"Current time: {now}"`
      from position #2 to the **last** block, after `_TOOL_GUIDANCE`. Rename
      the docstring "Cache-stable block order" comment to reflect the new order.
- [x] `src/lite_horse/skills/manage_tool.py::dispatch` — before writing SKILL.md
      on `create`/`edit`/`write_file`, reuse `_INJECTION_PATTERNS` and
      `_INVISIBLE_RE` from `memory/store.py` (extract to
      `src/lite_horse/security/validators.py` — new module, ~30 LOC — so both
      sites import it). `memory/store.py` imports from the new module; no
      behavior change for memory.
- [x] `src/lite_horse/security/__init__.py` — empty.
- [x] `tests/test_instructions_assembly.py` — assert `Current time:` appears
      AFTER `TOOL USE GUIDANCE` in the rendered prompt.
- [x] `tests/test_skill_manage_tool.py` — add cases for
      `create` / `edit` / `write_file` with injection payloads
      (`"ignore all previous instructions"`, zero-width space) returning
      `{"success": False, "error": ...}` and NOT writing the file.

### Acceptance
- [x] All new tests green; existing 105 tests still green (110 total)
- [x] `ruff`, `mypy --strict` clean
- [ ] Manual: prompt diff between turn 1 and turn 2 of the same session is
      only the bottom `Current time:` line

### Risk / notes
- The cache retention is already set (`prompt_cache_retention="24h"`). This
  is the missing piece that lets the cache actually hit.
- If SOUL, MEMORY, or USER change mid-session (they won't — frozen-snapshot
  invariant), cache still busts; this phase only removes the time cliff.

**Est. LOC:** ~50 changed, ~50 test.

---

## Phase 14 — `skill_view` tool + kill dead `make_skills_capability`

**Objective:** implement real Level-1 progressive disclosure so the agent can
load a SKILL.md on demand instead of carrying every skill body in the prompt.
**Why:** `instructions.py` already tells the model to call `skill_view(name)`
— but the tool does not exist. Simultaneously, `skills/source.py::make_skills_capability()`
imports `Skills` / `LocalDirLazySkillSource` but is never passed into
`Agent(...)`; the SDK's `Agent.__init__` has no `capabilities` param. Both
issues trace back to a v0.1 plan assumption that was never wired.
**Depends on:** 13 (injection defense is required — `skill_view` returns
content that lands in the model's context).

### Deliverables
- [x] `src/lite_horse/skills/view_tool.py` — new module:
      ```python
      @function_tool(name_override="skill_view",
                     description_override="Load the full SKILL.md for <name>. "
                                          "Call this after seeing the skill in "
                                          "the AVAILABLE SKILLS index and before "
                                          "doing related work.")
      async def skill_view(ctx, name: str) -> str: ...
      ```
      Returns SKILL.md text (capped at 16 KB — one char above the 15 KB skill
      size limit). Slug-validated via the same `_SLUG_RE` from
      `manage_tool.py` (extract to `skills/_slug.py`, 5 LOC). Path-traversal
      guarded. Returns JSON `{"success": False, "error": ...}` for missing
      skill or invalid name.
- [x] `src/lite_horse/agent/factory.py` — include `skill_view` in the always-on
      tools list next to `memory_tool`, `session_search`, `skill_manage`.
- [x] `src/lite_horse/skills/source.py` — delete `make_skills_capability`
      and the `Skills` / `LocalDirLazySkillSource` / `LocalDir` imports. Keep
      `skills_root()` and `sync_bundled_skills()`.
- [x] `tests/test_skill_view_tool.py` — happy path, missing skill, slug
      violation, path-traversal attempt, content cap.

### Acceptance
- [x] `_skills_index()` still lists names + descriptions only (Level 0).
- [x] Agent can call `skill_view("plan")` and receive the full bundled skill.
- [x] `grep -r make_skills_capability src/ tests/` returns nothing.

**Est. LOC:** +60 runtime / +90 test / −15 dead.

---

## Phase 15 — drops: gateway, CLI REPL, systemd, Telegram dep

**Objective:** remove everything that exists only to service standalone /
chat-platform use cases. The webapp is now the sole consumer.
**Why:** lightweight invariant; fewer moving parts to secure and test.
**Depends on:** none (drops are orthogonal to 13 / 14). Recommend running
after 14 so dead-code audit is accurate.

### Deliverables — code removal
- [x] Delete `src/lite_horse/gateway/` entirely EXCEPT `session_key.py` —
      move `gateway/session_key.py` to `src/lite_horse/core/session_key.py`
      and update the one import site (`gateway/runner.py` is being deleted, so
      the new caller is `api.py` from Phase 16).
- [x] Delete `src/lite_horse/gateway/` directory after the move.
- [x] Delete `src/lite_horse/cli.py`. Replace with a minimal
      `src/lite_horse/_debug_cli.py` (≤ 40 LOC) registered under the
      `litehorse-debug` script in `pyproject.toml` — for local dev only.
      Production consumers import `lite_horse.api`.
- [x] Delete `deploy/gateway.service` and `deploy/cron.service`.
- [x] Delete `deploy/` if empty after.
- [x] `src/lite_horse/cron/scheduler.py` — delete `deliver_telegram` and the
      `from telegram import Bot` import. Leave `deliver_log` as default;
      Phase 17 adds the webhook delivery.
- [x] `tests/test_gateway_session_key.py` → move assertions into
      `tests/test_core_session_key.py`.
- [x] Delete `tests/test_gateway_guard.py` and `tests/test_gateway_runner.py`.

### Deliverables — dependency trim
- [x] `pyproject.toml` — remove `python-telegram-bot`, `rich`, `click`.
- [x] `uv lock` regenerated; commit `uv.lock`.

### Deliverables — doc trim
- [x] `README.md` — rewrite top-to-bottom: positioning is "embeddable Python
      package", Quick Start imports `lite_horse.api.run_turn` from a Python
      REPL, removes Telegram/gateway/systemd sections. Keep MCP section.

### Acceptance
- [x] `grep -r 'import telegram\|python-telegram-bot\|rich\|click' src/ tests/`
      returns nothing.
- [x] `uv run pytest -q` still green (test count drops by ~20).
- [x] `cloc src/lite_horse/` ≤ 1,900 LOC (target hit before Phase 16 rebuilds
      part of what was removed).

**Est. LOC:** −450 runtime, −300 test.

---

## Phase 16 — `lite_horse.api` — the webapp-facing surface

**Objective:** one import the webapp uses. Everything else is internal.
**Why:** without this, every webapp caller reaches into `factory` + `SDKSession`
+ `SessionDB` + `Runner` directly; we lose the ability to refactor without
breaking the consumer.
**Depends on:** 15 (CLI deletion first so naming is unambiguous).

### Deliverables
- [x] `src/lite_horse/api.py` — single module, all public. Exports:
      ```python
      async def run_turn(
          *,
          session_key: str,          # e.g. build_session_key(platform="web", ...)
          user_text: str,
          source: str = "web",       # recorded in SessionDB
          user_id: str | None = None,
          max_turns: int | None = None,
      ) -> RunResult: ...

      async def end_session(session_key: str, *, reason: str = "user_exit") -> None: ...

      def search_sessions(query: str, *, limit: int = 20,
                          source: str | None = None) -> list[SearchHit]: ...

      @dataclass
      class RunResult:
          final_output: str
          session_key: str
          turn_count: int
          tool_calls: int
      ```
- [x] `src/lite_horse/core/__init__.py`, `src/lite_horse/core/session_key.py`
      (moved in 15).
- [x] `api.py` lazily constructs **one** process-wide `SessionDB` on first
      call, calls `bind_db` once, reuses the SDK `Agent` across calls (the
      SDK's `Agent` is stateless per-call; `build_agent` is cheap but
      cache-friendly to keep).
- [x] Concurrency: `run_turn` is re-entrant across distinct `session_key`s,
      but serialized per `session_key` via a process-local `asyncio.Lock`
      registry (port the per-key lock pattern from the deleted `gateway/guard.py`
      — lock-only, no interrupt queue this time). Put it in
      `src/lite_horse/core/session_lock.py` (~25 LOC).
- [x] `tests/test_api.py` — happy path, same-key serialization, distinct-key
      parallelism, `end_session` writes `ended_at`.
- [x] `src/lite_horse/_debug_cli.py` — now just calls `api.run_turn` in a
      loop. No `click`, no `rich` — plain `input()`/`print()`.

### Acceptance
- [x] Webapp-side integration: `from lite_horse.api import run_turn` is the
      only symbol needed for a full chat.
- [x] No module imports from `lite_horse.agent.factory`,
      `lite_horse.sessions.sdk_session`, or `lite_horse.sessions.db` outside
      of `api.py` and internal modules.

**Est. LOC:** +110 runtime, +80 test.

---

## Phase 17 — cron webhook delivery + job CRUD tool

**Objective:** keep cron as a Hermes best-feature (scheduled self-directed
runs) but route results into the webapp instead of Telegram.
**Why:** the webapp wants to surface "your daily standup draft", "nightly
project audit", etc. in its own UI/notification system.
**Depends on:** 16 (cron reuses `api.run_turn` semantics).

### Deliverables
- [x] `src/lite_horse/cron/delivery.py` — new module:
      ```python
      async def deliver_webhook(
          spec: dict[str, Any], text: str, session_key: str
      ) -> None:
          """POST {"text": ..., "session_key": ...} to spec['url'] with
             an HMAC-SHA256 signature in the X-LiteHorse-Signature header."""
      ```
      HMAC secret read from `LITEHORSE_WEBHOOK_SECRET` env. Retry: 3x with
      exponential backoff (1s / 4s / 16s) on 5xx or connection errors;
      4xx abort immediately and log. Use `httpx` (new dep; replaces the
      telegram Bot import so no net-new weight). Delivery signature threads
      `session_key` so the webapp can correlate.
- [x] `src/lite_horse/cron/scheduler.py` — extend `DELIVERY_HANDLERS` with
      `"webhook"` → `deliver_webhook`; deliver handlers now take
      `(spec, text, session_key)`. `build_agent` import deferred to avoid
      factory ↔ cron cycle.
- [x] `src/lite_horse/cron/manage_tool.py` — new `@function_tool cron_manage`
      exposing add / list / remove / enable / disable. Agent-callable so a
      user can say "every Monday 9am summarize open PRs" and the agent
      creates the job.
- [x] Register `cron_manage` in `factory.build_agent()` (always-on).
- [x] `pyproject.toml` — add `httpx>=0.27`.
- [x] `tests/test_cron_delivery.py` — webhook happy path with HMAC verified;
      retry on 503; abort on 400; transport-error retry exhaustion; missing
      secret refuses to post.
- [x] `tests/test_cron_manage_tool.py` — add/remove/list/enable/disable
      round-trip + rejection cases (bad schedule, non-http URL, unknown
      platform, missing required args).
- [x] Keep `deliver_log` for local dev.

### Acceptance
- [x] `Job.delivery = {"platform": "webhook", "url": "https://..."}` fires and
      hits the webapp endpoint with a valid HMAC signature.
- [x] The agent can create a cron job end-to-end via `cron_manage` in a test
      harness.

### Notes
- Do NOT ship any webhook URL in bundled config. Webapp configures it per job
  via `cron_manage`.
- `cron` worker runs as a **separate process** from the webapp's request
  serving (the webapp spawns it on boot, supervises it).

**Est. LOC:** +170 runtime (+40 replaces deleted telegram delivery), +100 test.

---

## Phase 18 — compression-as-consolidation

**Objective:** at the budget WARNING tier (90 %), distill the trajectory into
MEMORY.md before the hard stop, so durable context survives truncation.
**Why:** v0.1 only *warns*. Hermes `context_compressor.py` consolidates
knowledge into memory layers before truncation — this is the difference
between "ran out of turns" and "ran out of turns AND lost what we learned".
**Depends on:** 13 (injection defense must cover distiller-written memory;
`MemoryStore.add` already does, but verify).

### Deliverables
- [x] `src/lite_horse/agent/consolidator.py` — new module:
      ```python
      class Consolidator:
          """Side-agent that distills the last N turns into memory entries."""
          async def run(self, *, turn_input: list[dict]) -> list[str]:
              """Returns entries the distiller wants to add. Caller applies
                 them via MemoryStore.add(), swallowing MemoryFull /
                 UnsafeMemoryContent."""
      ```
      Side-agent instructions: "Read the trajectory below. Identify ≤5 durable
      facts (user preference / project convention / environmental constant)
      worth surviving truncation. Each entry ≤ 200 chars. Return JSON array."
      `model=cfg.model`, `max_turns=3`.
- [x] `src/lite_horse/agent/budget.py::BudgetHook` — on transition to
      `WARNING` tier, fire the consolidator ONCE per run. Exceptions
      swallowed — consolidation must not break the run (same rule as
      `EvolutionHook`).
- [x] `tests/test_consolidator.py` — mocked Runner returns a JSON array;
      entries end up in `MemoryStore`; MemoryFull is swallowed; injection-flagged
      entries are dropped with a log line but the run continues.

### Acceptance
- [x] Trajectory-ending test: drive the agent to 90 % budget with a fake
      model; assert `MEMORY.md` contains ≥1 new entry written by the
      consolidator before final output.

### Notes
- Only fires at the **WARNING** tier, not the CAUTION tier — we don't want
  to pay for a distiller run every time the model does some work.
- The distiller writes via `memory_tool` would double the call surface;
  direct `MemoryStore.add` is cheaper and uses the same validation path.

**Est. LOC:** +120 runtime, +90 test.

---

## Phase 19 — periodic memory nudge

**Objective:** inject a lightweight "consider persisting anything durable
now" note into the tool-output stream every N=10 tool calls.
**Why:** Hermes' periodic nudge is distinct from its budget pressure — it's
about *persistence decisions*, not about winding down. Today we only signal
stop, never "time to remember".
**Depends on:** 13 (prompt cache fix ensures nudge doesn't bust cache — it
gets appended to tool-result content, same mechanism as budget notes).

### Deliverables
- [x] `src/lite_horse/agent/budget.py` — extend `BudgetHook` with a fourth
      signal: every `NUDGE_EVERY_N_TOOL_CALLS = 10` iterations, inject a
      one-shot nudge (idempotent per N-bucket so it never fires twice at
      iteration 10).
      ```text
      [NUDGE: You have done 10 tool calls. If you learned a durable fact —
       user preference, environmental constant, lesson — consider
       `memory(action='add', ...)` before continuing.]
      ```
- [x] `src/lite_horse/constants.py` — add `NUDGE_EVERY_N_TOOL_CALLS = 10`.
- [x] `tests/test_budget_hook.py` — assert nudge injected at iteration 10,
      not again at iteration 11-19, again at iteration 20; assert it coexists
      with CAUTION/WARNING tiers (different note text).

### Acceptance
- [x] Test run with 25 tool calls shows exactly 2 nudges + 1 CAUTION +
      1 WARNING note, in that order.

**Est. LOC:** +50 runtime, +40 test.

---

## Phase 20 — skill stats sidecar + in-use refinement

**Objective:** track usage / success / error per skill, and on failure,
propose a patch via side-agent ("skill refinement during use", the
second Hermes verb after "create").
**Why:** v0.1 only creates skills; it never improves them. Stats also feed
Phase 24's offline evolve loop — can't optimize skills you have no data on.
**Depends on:** 14 (`skill_view` is how "skill used" is observed).

### Deliverables
- [x] Sidecar format: `~/.litehorse/skills/<name>/.stats.json`:
      ```json
      {
        "usage_count": 0,
        "success_count": 0,
        "error_count": 0,
        "last_used_at": null,
        "last_error_at": null,
        "last_error_summary": null,
        "last_optimized_at": null,
        "schema_version": 1
      }
      ```
- [x] `src/lite_horse/skills/stats.py` — new module: `record_view(name)`,
      `record_outcome(name, ok: bool, error_summary: str | None)`,
      `read(name)`. File-locked writes (use `fcntl` on posix,
      no-op on windows — but we target linux-only for the embed).
- [x] `src/lite_horse/skills/view_tool.py` — on successful read, call
      `record_view(name)`.
- [x] `src/lite_horse/agent/evolution.py::EvolutionHook`:
      1. Track which skills were viewed in the current run (from
         tool-call log).
      2. On `on_end`: if trajectory ended with model-emitted error
         markers ("error", "failed", exception tracebacks in tool results)
         AND a skill was viewed, call `record_outcome(name, ok=False, ...)`.
         Otherwise `ok=True`.
      3. If outcome is failure on a viewed skill, spawn a **refiner
         side-agent** (distinct from distiller): "Given this trajectory and
         this SKILL.md, propose ONE `skill_manage(action='patch', ...)` call
         that would have avoided the failure. Conservative — if unclear, do
         nothing."
      4. Swallow all errors.
- [x] `src/lite_horse/agent/instructions.py::_skills_index()` — when
      rendering a skill line, append a decay tag if `error_count >= 3` AND
      `success_count / max(1, usage_count) < 0.5`: `(fragile — see stats)`.
- [x] `tests/test_skills_stats.py` — concurrent writes don't clobber;
      record_outcome updates correct fields; sidecar created on first write.
- [x] `tests/test_evolution_hook.py` — refiner runs on failure-with-viewed-skill;
      does NOT run on success; does NOT run on failure-without-skill-use.

### Acceptance
- [x] 100 sequential runs exercising a skill yield a sidecar with
      `usage_count == 100` and sane success / error counts.

### Notes
- Skill failure detection via string markers is heuristic; that's fine for
  a nudge signal. Phase 22's error classifier later upgrades this to a
  structured signal.

**Est. LOC:** +160 runtime, +120 test.

---

## Phase 21 — conditional skill activation (agentskills.io compat)

**Objective:** stop dumping every skill in the prompt. Honor
`activate_when:` frontmatter; filter skills by match against the latest user
message.
**Why:** today `_skills_index()` lists every skill unconditionally. At 20+
skills this is pure prompt bloat. agentskills.io defines a standard `trigger`
/ `activate_when` field for this.
**Depends on:** 13, 14, 20.

### Deliverables
- [x] Extend SKILL.md frontmatter schema to include:
      ```yaml
      activate_when:
        - keywords: ["deploy", "ship", "release"]
        - file_globs: ["Dockerfile", "k8s/*.yaml"]
      category: devops
      ```
      Parse via `yaml.safe_load` on the frontmatter block.
- [x] `src/lite_horse/skills/activation.py` — new module:
      ```python
      def filter_for_turn(
          *,
          skills_dir: Path,
          user_text: str | None,
          top_k: int = 8,
      ) -> list[SkillEntry]: ...
      ```
      Heuristic: keyword match scores 2x, presence in recent `USER.md` scores
      1x, default-always-on (no `activate_when`) scores 0.5x. Return top-K
      sorted. NO embedding model — too heavy for a per-turn call.
- [x] `src/lite_horse/agent/instructions.py::make_instructions` — grab the
      most recent user message from `ctx.turn_input` (safely, same pattern
      as `EvolutionHook._extract_user_request`), pass to
      `activation.filter_for_turn`. Fallback to all-skills when
      extraction fails.
- [x] Update bundled `plan/SKILL.md` and `skill-creator/SKILL.md` with
      `activate_when` examples. Keep them always-on (no frontmatter key).
- [x] `tests/test_skill_activation.py` — keyword match, file-glob match,
      top-K cap, fallback behavior.
- [x] `tests/test_instructions_assembly.py` — prompt for "deploy the app"
      includes the devops skill; prompt for "write a poem" does not.

### Acceptance
- [x] A library of 25 fake skills renders only ≤ 8 in the prompt for any
      given user message.

### Notes
- Activation is best-effort: the agent can always `skill_view(name)` anything
  it knows exists. The stats sidecar from Phase 20 can also surface skills
  that have high success on the current task cluster.

**Est. LOC:** +120 runtime, +100 test.

---

## Phase 22 — structured error classifier

**Objective:** replace `except Exception: log.exception(...)` with a
classifier that tags failures and drives differentiated handling.
**Why:** rate-limit should retry; model refusal should surface; context
overflow should trigger consolidation (Phase 18); tool errors should feed
Phase 20's refiner. Mixing them loses all those signals.
**Depends on:** 16, 17, 18.

### Deliverables
- [x] `src/lite_horse/agent/errors.py` — new module:
      ```python
      class ErrorKind(enum.StrEnum):
          RATE_LIMIT = "rate_limit"
          CONTEXT_OVERFLOW = "context_overflow"
          MODEL_REFUSAL = "model_refusal"
          TOOL_ERROR = "tool_error"
          NETWORK = "network"
          UNKNOWN = "unknown"

      @dataclass
      class ClassifiedError:
          kind: ErrorKind
          retryable: bool
          original: BaseException
          summary: str

      def classify(exc: BaseException) -> ClassifiedError: ...
      ```
      Dispatches on `openai.RateLimitError`, `openai.APIConnectionError`,
      `openai.BadRequestError` (context overflow substring check), SDK
      exception types, etc.
- [x] `src/lite_horse/api.py::run_turn` — wrap `Runner.run`, on
      `RATE_LIMIT`/`NETWORK` retry up to 3x with backoff, on
      `CONTEXT_OVERFLOW` log and return a graceful error, on
      `MODEL_REFUSAL` surface the message.
- [x] `src/lite_horse/cron/scheduler.py::make_fire` — use `classify` to
      decide whether to retry the firing on next tick or permanently
      disable the job (3 strikes on `MODEL_REFUSAL` → job disabled with
      reason stamped in JobStore).
- [x] `tests/test_errors.py` — each `ErrorKind` dispatched from a synthetic
      exception; classifier is total (no `UNKNOWN` leak for known types).

### Acceptance
- [x] Injected rate-limit during `run_turn` results in 1 successful return
      after 2 retries; injected model refusal surfaces on first call.

**Est. LOC:** +110 runtime, +80 test.

---

## Phase 23 — config.mcp_servers + token_count cleanup

**Objective:** first-class MCP attachment via config, and remove dead
`token_count` column on `messages`.
**Depends on:** 16.

### Deliverables
- [x] `src/lite_horse/config.py` — add:
      ```yaml
      mcp_servers:
        - name: pm-broker
          url: http://localhost:7444/mcp
          cache_tools_list: true
      ```
      Pydantic model with `name`, `url`, `cache_tools_list: bool = True`.
      Reject URLs with scheme other than `http` / `https`.
- [x] `src/lite_horse/agent/factory.py::build_agent` — instantiate
      `MCPServerStreamableHttp` for each, pass via `mcp_servers=[...]`
      kwarg (SDK 0.14 supports it on `Agent.__init__`).
- [x] MCP server lifetimes — process-level: `api._ensure_ready` connects
      each server once on first `run_turn` (cache_tools_list then actually
      caches across turns); `api.shutdown()` cleans up.
- [x] `src/lite_horse/sessions/db.py::_SCHEMA` — drop `token_count` column
      via `ALTER TABLE DROP COLUMN` (SQLite ≥ 3.35); schema bumped 1 → 2.
      `append_message` signature loses `token_count`.
- [x] `tests/test_session_db_fts5.py` — migration test: v1 DB upgrades
      cleanly on open.
- [x] `README.md` — replaced the inline MCP recipe with "configure in
      `config.yaml`".

### Acceptance
- [x] `config.yaml` with one MCP server results in its tools appearing in
      a live agent run (wired end-to-end; verified via factory + api tests).

**Est. LOC:** +80 runtime, +60 test.

---

## Phase 24 — offline evolve pipeline (`lite_horse.evolve`)

**Objective:** the GEPA-equivalent offline loop. Reads SessionDB traces,
mutates SKILL.md candidates, gates on pytest + size + semantic similarity,
writes **proposals** for human approval — never auto-edits.
**Why:** mirrors `nousresearch/hermes-agent-self-evolution` without pulling
DSPy into the runtime. Executes as a separate script invoked by the webapp
(button in admin UI) or by the webapp's own cron.
**Depends on:** 20 (stats drive prioritization), 22 (error kinds let us
mine failure traces).

### Deliverables — module layout
- [x] `src/lite_horse/evolve/__init__.py`
- [x] `src/lite_horse/evolve/trace_miner.py` — reads last N days of sessions
      from `SessionDB`, extracts `(task, response, outcome)` triples. Outcome
      is derived from `sessions.end_reason` (error-kind names persisted at
      session close) + the skill's stats sidecar last_error_summary.
- [x] `src/lite_horse/evolve/fitness.py` — LLM-as-judge rubric (0-1 per
      sample) + length penalty + `text-embedding-3-small` cosine against the
      baseline SKILL.md.
- [x] `src/lite_horse/evolve/constraints.py` — hard gates:
      - pytest suite passes (injectable runner)
      - size ≤ 15 KB (`SKILL_MAX_BYTES`)
      - semantic cosine ≥ 0.75 vs. baseline (catches purpose drift)
      - no `_INJECTION_PATTERNS` / `_INVISIBLE_RE` hits
      - frontmatter valid YAML, preserves `name:` and bumps `version:`
- [x] `src/lite_horse/evolve/reflector.py` — single-step LLM reflection.
      Commentary above the fence is stripped before validation. No DSPy dep.
- [x] `src/lite_horse/evolve/runner.py` — CLI-free `evolve(skill_name)`
      entry. Injectable reflector/judge/embedder/pytest_runner for tests.
- [x] `src/lite_horse/evolve/cli.py` + `__main__.py` —
      `python -m lite_horse.evolve <skill>` admin shim.
- [x] `tests/test_evolve_constraints.py` — each gate isolated (11 tests).
- [x] `tests/test_evolve_e2e.py` — end-to-end with stubbed LLM + synthetic
      traces, rejection paths, CLI smoke, import-graph guard (8 tests).

### Deliverables — surface in webapp
- [x] `~/.litehorse/skills/.proposals/<skill>/<ts>.md` + `<ts>.json` with
      `{"fitness": {...}, "gates": {...}, "trajectories": [...]}`. The on-disk
      contract the webapp renders against is now fixed.
- [ ] The webapp lists pending proposals and exposes approve / reject.
      Approve = move the candidate to `~/.litehorse/skills/<skill>/SKILL.md`
      and stamp `last_optimized_at` via `skills.stats.mark_optimized`.
      *(Webapp-side; not in this repo.)*

### Acceptance
- [x] `python -m lite_horse.evolve <skill>` runs against a seeded SessionDB
      (covered by `test_evolve_e2e.py::test_evolve_happy_path_writes_proposal`
      and `::test_cli_smoke`); each gate's rejection path is covered.
- [x] `import lite_horse.api` does **not** transitively import
      `lite_horse.evolve`. Asserted by
      `tests/test_evolve_e2e.py::test_api_does_not_transitively_import_evolve`
      and by `python -c "import sys, lite_horse.api; assert 'lite_horse.evolve' not in sys.modules"`.

### Notes
- Do **not** add DSPy or GEPA as runtime deps. If we want them, they go in
  an optional `[project.optional-dependencies] evolve = [...]` extra.
- Baseline approach is single-pass LLM reflection; upgrade to GEPA later if
  the single-pass isn't finding useful mutations after real usage.

**Est. LOC:** +350 (separate module) / +180 test.

---

## Phase 25 — final hardening & docs

**Objective:** green-light v0.2 for embed.
**Depends on:** every preceding v0.2 phase.

### Deliverables
- [x] `ruff check src tests` clean
- [x] `mypy src` (strict) clean — 45 source files
- [x] `pytest -q` clean — **245 tests** (plan estimated ~180; came in higher)
- [~] `cloc src/lite_horse/` ≤ 2,000 lines of runtime Python (excludes
      `lite_horse/evolve/`). **Actual ~2,720 non-blank/non-`#` lines.**
      Accepted as a documented miss — hitting 2,000 would require a real
      refactor with no clear fat (biggest files: `sessions/db.py` 370,
      `agent/evolution.py` 246, `cron/scheduler.py` 206, `api.py` 202).
      Deferred to a future cleanup phase if operationally warranted.
- [x] `README.md` reflects the embedded-package story end-to-end with a
      working `lite_horse.api.run_turn` quickstart.
- [x] `docs/EMBEDDING.md` — webapp integration contract: env vars,
      `config.yaml`, MCP, cron webhook body + HMAC header.
- [x] `docs/EVOLVE.md` — offline loop, gate reference, proposal review
      workflow.
- [x] Plan flipped to `**Status:** SHIPPED` with shipped date; v0.3 plan
      does not yet exist, so `PROGRESS.md`'s "Active plan" link stays
      pointing at this file (now marked SHIPPED) until v0.3 is drafted.

### Acceptance
- [ ] One full round-trip from the PM webapp: user sends a message, agent
      calls a tool, response streams back, session persists, `session_search`
      finds the turn, `cron_manage` creates a recurring job, webhook
      delivery fires and reaches the webapp. **Verified out-of-band: lives
      in the PM webapp repo, not here.**
- [ ] `lite_horse.evolve` run against seeded data produces ≥1 approved
      proposal and it's merged via the webapp's review UI. **Verified
      out-of-band: requires the webapp's admin UI.**

---

## Appendix A — constants changing in v0.2

| Constant | v0.1 | v0.2 | Set in |
|---|---|---|---|
| `SCHEMA_VERSION` | 1 | 2 | `constants.py` (Phase 23) |
| `NUDGE_EVERY_N_TOOL_CALLS` | — | 10 | `constants.py` (Phase 19) |
| `SKILL_MAX_BYTES` | implicit | 15 360 | `constants.py` (Phase 24) |
| `ACTIVATION_TOP_K` | — | 8 | `constants.py` (Phase 21) |
| Prompt block order | SOUL → TIME → MEM → USR → SKILLS → AGENTS → GUIDANCE | SOUL → MEM → USR → SKILLS → AGENTS → GUIDANCE → TIME | `instructions.py` (Phase 13) |

## Appendix B — file-level diff summary

```
DELETE
  src/lite_horse/cli.py                         [15]
  src/lite_horse/gateway/                       [15, move session_key first]
  deploy/                                       [15]
  tests/test_gateway_*.py (3 files)             [15]

MOVE
  gateway/session_key.py → core/session_key.py  [15]

ADD
  src/lite_horse/api.py                         [16]
  src/lite_horse/core/                          [15/16]
  src/lite_horse/core/session_lock.py           [16]
  src/lite_horse/security/validators.py         [13]
  src/lite_horse/skills/view_tool.py            [14]
  src/lite_horse/skills/stats.py                [20]
  src/lite_horse/skills/activation.py           [21]
  src/lite_horse/agent/consolidator.py          [18]
  src/lite_horse/agent/errors.py                [22]
  src/lite_horse/cron/delivery.py               [17]
  src/lite_horse/cron/manage_tool.py            [17]
  src/lite_horse/evolve/                        [24]
  src/lite_horse/_debug_cli.py                  [15]
  docs/EMBEDDING.md                             [25]
  docs/EVOLVE.md                                [25]

MODIFY
  src/lite_horse/agent/factory.py               [14, 17, 23]
  src/lite_horse/agent/instructions.py          [13, 21]
  src/lite_horse/agent/budget.py                [18, 19]
  src/lite_horse/agent/evolution.py             [20]
  src/lite_horse/skills/manage_tool.py          [13]
  src/lite_horse/skills/source.py               [14]
  src/lite_horse/sessions/db.py                 [23]
  src/lite_horse/cron/scheduler.py              [15, 17, 22]
  src/lite_horse/config.py                      [23]
  src/lite_horse/constants.py                   [19, 21, 23, 24]
  pyproject.toml                                [15, 17]
  README.md                                     [15, 25]
```

## Appendix C — test matrix growth

| File | Status |
|---|---|
| `test_session_db_fts5.py` | modified (schema v2 migration) |
| `test_memory_store.py` | unchanged |
| `test_skill_manage_tool.py` | modified (+ injection cases) |
| `test_evolution_hook.py` | modified (+ refiner logic) |
| `test_budget_hook.py` | modified (+ nudge + consolidator gate) |
| `test_instructions_assembly.py` | modified (+ cache-stable order, conditional skills) |
| `test_api.py` | new |
| `test_core_session_key.py` | new (renamed from gateway) |
| `test_errors.py` | new |
| `test_cron_delivery.py` | new |
| `test_cron_manage_tool.py` | new |
| `test_skill_view_tool.py` | new |
| `test_skill_activation.py` | new |
| `test_skills_stats.py` | new |
| `test_consolidator.py` | new |
| `test_evolve_constraints.py` | new |
| `test_evolve_e2e.py` | new |
| `tests/e2e/test_chat_roundtrip.py` | modified (api-based, no CLI) |
| `tests/test_gateway_*.py` | DELETED |

## Appendix D — sprint plan

| Sprint | Phases | Outcome |
|---|---|---|
| 1 | 13, 14, 15 | Prompt-cache fixed, `skill_view` wired, gateway/CLI dropped |
| 2 | 16, 17 | Webapp-facing API + cron webhook delivery |
| 3 | 18, 19, 22 | Consolidation, nudges, error classifier |
| 4 | 20, 21, 23 | Skill stats & refinement, conditional activation, MCP config |
| 5 | 24 | Offline evolve pipeline |
| 6 | 25 | Hardening, docs, green-light |

**Estimated total delta:** +1,400 runtime LOC added, −450 dropped,
−300 tests dropped, +900 tests added. Net runtime: ~2,100 LOC (up slightly
from v0.1's 2,115 because we're adding real self-evolution, but the drops
paid for the additions and we stay well under the 4,000-line ceiling).
