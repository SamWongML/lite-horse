# Copy this file to ~/.hermeslite/.env (or set HERMESLITE_HOME) and fill in values.

# Required for the agent to call OpenAI.
OPENAI_API_KEY=

# Required only when gateway.telegram.enabled is true.
TELEGRAM_BOT_TOKEN=
